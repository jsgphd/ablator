import gtk

#class AblatorOpacdataPanel (gtk.VBox):
   
    #def __init__(self):

        #gtk.VBox.__init__(self)

        #for klic, hodnota in self.entries().iteritems():

            #hbox = gtk.HBox(True, 0)
            #label = gtk.Label(klic)
            #hbox.pack_start(label, False, False, 0)
            #hbox.pack_start(hodnota, False, False,0)
            #self.pack_start(hbox)
            

    #def entries (self):

        #es = {}
        #es["opacdata"] = gtk.Entry()
        #es["opac"] = gtk.Entry()
        #es["data"] = gtk.Entry()
        #es["nevim"] = gtk.Entry()
        #es["dfasd"] = gtk.Entry()
        #es["gdfs"] = gtk.Entry()
        #es["fgsdg"] = gtk.Entry()

        #return es
